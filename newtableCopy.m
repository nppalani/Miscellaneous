for i = 1:10928
   % newtab(i).barcodeid = blastruct(i,1).barcodeid;
    % newtab(i).pident = blastresultoutput(i,2).pident;
     % newtab(i).evalue = blastresultoutput(i,3).evalue;
     newtab(i) = barstruct(i);
end
